#!/usr/bin/python3
num = 77
def prime(x):

for x in range(2, num - 1, -1):
    if x % x == 0 and num % x = 0:
        y = num / x
        if y % y == 0 and y % 1 == 0:
            print(f"{num}={x}x{y}")
        else:
            continue
